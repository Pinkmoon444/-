pwd (отображает путь, в котором мы сейчас находимся)

ls (показать список файлов и папок)
    ls -a (в том числе скрытые)

cd ((change directory) поменять папку)
    cd имя_папки  (перейти в папку(если начать писать имя папки, а потом нажать Tab, то название папки допишется автоматически(если другой такой папки не существует)))
    cd .. (перейти на папку выше, назад)
    cd ((без аргументов)перейти в домашнюю папку)

mkdir имя_папки (создать папку)
touch имя_файла (создать файл)
nano имя_файла (открыть этот файл в редакторе)
    Ctrl+ S (сохранить)
    Ctrl + x (закрыть)


python имя_файла (запустить файл в питоне)

; чтобы установить дополнительные модули из файла за 1 команду нужно:
- сохранить их все в файл (нампример, "импорт.txt")
- открыть в терминале вкладку с этим файлом (через команды cd)
- написать pip install -r имя_файла


; чтобы обновить код на удалнном от нас сервере, нужно:
- доработать код на локальном компутере и синхронизировать его с гитхабом
- на удалённом от нас терминале зайти в папку с проектом (через команды cd)
- выполнить команду git pull
